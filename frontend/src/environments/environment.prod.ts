export const environment = {
  production: true,
  // Replace this with your Render backend URL after Render creates the web service,
  // for example: https://leave-management-api.onrender.com/api
  // Then commit this file and redeploy the Render static site.
  apiBaseUrl: 'https://YOUR-RENDER-BACKEND-URL.onrender.com/api'
};
